# 한국 주식시장 마감 분석 — 2026년 7월 17일 (KST)

> **⚠️ 데이터 수집 실패**: 오늘 마감 분석은 실시간 데이터를 가져오지 못했습니다.

---

## 데이터 수집 결과

| 항목 | 상태 | 사유 |
|------|------|------|
| KOSPI / KOSDAQ / KOSPI200 | ❌ 실패 | `m.stock.naver.com` 프록시 차단 (403 Forbidden) |
| 시장 폭 (등락 종목수) | ❌ 실패 | `finance.naver.com` 프록시 차단 (403 Forbidden) |
| 상승/하락 상위 종목 | ❌ 실패 | `finance.naver.com` 프록시 차단 (403 Forbidden) |
| 섹터 동향 | ❌ 실패 | `finance.naver.com` 프록시 차단 (403 Forbidden) |
| 워치리스트 10개 종목 | ❌ 실패 | `m.stock.naver.com` 프록시 차단 (403 Forbidden) |
| 해외 지수 (S&P500, NASDAQ, 닛케이, 상하이) | ❌ 실패 | `yfinance` / Yahoo Finance 프록시 차단 (403 Forbidden) |
| 환율 (USD/KRW, JPY/KRW) | ❌ 실패 | `yfinance` 프록시 차단 (403 Forbidden) |
| 원자재 (WTI, 금) | ❌ 실패 | `yfinance` 프록시 차단 (403 Forbidden) |

---

## 원인 분석

실행 환경(원격 컨테이너)의 아웃바운드 네트워크 정책이 다음 도메인에 대한 HTTPS 터널링을 차단합니다:

- `*.naver.com` / `finance.naver.com` / `m.stock.naver.com`
- `query1.finance.yahoo.com` (yfinance 내부 엔드포인트)
- 기타 외부 금융 데이터 소스

모든 요청이 프록시에서 `403 Forbidden`으로 거부되어 시장 데이터를 한 건도 수집하지 못했습니다.

---

## 조치 권고

1. **데이터 소스 교체**: 프록시 허용 목록에 있는 데이터 소스(KRX 공식 API, 한국투자증권 Open API 등)로 전환
2. **네트워크 정책 조정**: 실행 환경에서 위 도메인에 대한 아웃바운드 접근 허용 요청
3. **사전 수집 방식**: 로컬 환경에서 데이터를 수집한 뒤 컨테이너에 주입하는 파이프라인 구성
4. **대안 라이브러리**: `pykrx` (KRX 직접 접근) 또는 증권사 API 검토

---

*보고서 생성: 2026-07-17 (자동화 루틴) — 실데이터 없음*
